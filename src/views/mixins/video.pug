mixin video(video)
    div 
        h4
            a(href=`/videos/${video.id}`)=video.title
            p=video.hashtags
        small=video.createdAt 
        p=video.description
        p #{video.meta.rating} ratings
        p #{video.meta.views} views
        hr
